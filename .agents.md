# Repo Fallback Instructions

This repository primarily uses `AGENTS.md` as the Codex instruction file.

If your Codex setup is configured to treat `.agents.md` as a fallback instructions filename, use this file only as a short pointer:

- primary repo instructions live in `AGENTS.md`
- project-scoped Codex config lives in `.codex/config.toml`
- public architecture docs live in `README.md`, `spec.md`, `schemas.md`, `workflow-roles.md`, and `tool-contracts.md`

Keep this file short to avoid duplicate instruction context.
